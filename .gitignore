# Environment and Secrets — NEVER commit these
.env
.env.*
*.env.local

# Terraform — state and vars contain secrets
terraform/.terraform/
# Note: .terraform.lock.hcl is intentionally committed for reproducibility
terraform/terraform.tfstate
terraform/terraform.tfstate.*
terraform/*.tfvars
!terraform/*.tfvars.example
terraform/.terraform.tfstate.lock.info
terraform/crash.log
terraform/crash.*.log
terraform/override.tf
terraform/override.tf.json
terraform/*_override.tf
terraform/*_override.tf.json
*.tfplan
*.tfplan.json
terraform/tfplan
outputs.json

# Terraform state in root (shouldn't exist but ignore just in case)
terraform.tfstate
terraform.tfstate.*

# Aura token cache
.cache/neo4j-aura-token.json

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST
.pytest_cache/
.mypy_cache/
.dmypy.json
dmypy.json
.ruff_cache/
.coverage
htmlcov/

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Logs
*.log
logs/
kafka-connect/logs/

# Demo / Presentation materials (may contain sensitive info)
DEMO_PLAYBOOK.md

# Docker & Kafka Connect
kafka-connect/plugins/*.jar
kafka-connect/plugins/*.zip
kafka-connect/connectors/*.json
kafka-connect/connect-distributed.properties

# Temporary files
*.tmp
*.bak
temp/
tmp/

# OS
.DS_Store
Thumbs.db

# Context repos
neo4j/
neo4j-cloud/

# Repomix output
repomix-output.*

# Gitleaks report
gitleaks-report.*

# Cursor / Claude
.claude/
.claude/settings.local.json
.cursor/
